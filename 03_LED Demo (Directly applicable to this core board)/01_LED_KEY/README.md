# 01_LED_KEY

Проект: **Управление светодиодами с двух кнопок**  
Плата: **Cyclone 10 LP (например 10CL006YE144C8G)**  
Категория: 03_LED Demo (Directly applicable to this core board)

---

## 🔧 Описание

Данный проект реализует простую, но полнофункциональную систему:
- Фильтрация дребезга двух кнопок.
- Подсчёт нажатий: одна кнопка увеличивает счётчик, другая — уменьшает.
- Отображение результата на двух светодиодах.

Идеально подходит для отладочных плат на базе Cyclone 10 LP, без необходимости подключения внешних устройств.

---

## 🚀 Особенности

- Модуль `key_filter.v` — осуществляет подавление дребезга кнопок и выдаёт чистый единичный импульс при нажатии или отпускании.
- Модуль `led_ctrl.v` — реализует логику счётчика, который увеличивается или уменьшается по событию кнопки, и связывает выход с двумя светодиодами.
- Модуль `key_led_top.v` — объединяет всё вместе: тактовый сигнал, сброс, две кнопки и два светодиода.
- Содержит тестбенчи `key_filter_tb.v`, `key_led_top_tb.v` и модель кнопки `key_model.v` для симуляции дребезга и проверки правильности работы.

---

## 🧩 Сигналы и подключение

| Сигнал     | Назначение                        | Подключение на плате     |
|------------|-----------------------------------|--------------------------|
| `Clk`      | Тактовый сигнал (50 МГц)         | системный генератор      |
| `Rst_n`    | Сброс (активный низкий)          | Кнопка Reset или GPIO    |
| `key_in0`  | Первая кнопка (увеличение)       | Кнопка SW0 / SW2         |
| `key_in1`  | Вторая кнопка (уменьшение)       | Кнопка SW1 / SW3         |
| `led[1:0]` | Два светодиода результата        | LED1 и LED2 на плате     |

---

## 📄 Содержание папки

01_LED_KEY/
├── rtl/
│ ├── key_filter.v
│ ├── led_ctrl.v
│ └── key_led_top.v
├── testbench/
│ ├── key_model.v
│ ├── key_filter_tb.v
│ └── key_led_top_tb.v
└── README.md ← (этот файл)

## 🧪 Симуляция

- Установите `timescale 1ns/1ns` и период такта `20 нс` (`50 МГц`) в тестбенчах.
- Запустите `key_filter_tb.v` и `key_led_top_tb.v` в симуляторе (ModelSim/QuestaSim).
- Наблюдайте:
  - для `key_filter_tb` — что при нажатии и отпускании кнопки выход `key_flag` вырабатывает ровно один импульс;
  - для `key_led_top_tb` — что счётчик на `led[1:0]` увеличивается/уменьшается как описано.

---

## 📌 Как начать

1. Откройте проект в Quartus Prime (например, версия 18.1 или выше).
2. Настройте `qsf`-файл: назначьте выводы кнопок и светодиодов согласно вашей плате.
3. Скомпилируйте и загрузите на плату.
4. Нажимайте первую кнопку — светодиоды должны отсчитывать вперёд.
5. Нажимайте вторую кнопку — светодиоды отсчитывают назад.

---

## 👥 Участие и развитие

> Проект открыт для сотрудничества.  
> Если хотите предложить улучшения — например, добавить режим автоматического мигания, поддержку третьей кнопки, UART-индикатор или расширение до SPI/I²C — создавайте пулл-запросы или открывайте темы.

---

© 2025 — проект «01_LED_KEY»  
Разработано при участии команды **AIDevelopersMonster** и соавторов.

